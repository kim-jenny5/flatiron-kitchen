<h2>New Recipe</h2>
<%= form_for recipe do |f| %>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
    <br><br>
    
    <h3>Ingredients</h3>
    <%= f.collection_check_boxes :ingredient_ids, Ingredient.all, :id, :name %>

    <p><%= f.submit %></p>
<% end %>